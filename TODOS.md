# TODOS

Deferred items from `/plan-ceo-review` and `/plan-eng-review` on moamoa (2026-09-14).
Each was explicitly deferred by the user during the review — not silently dropped.

## 1. ContestAdminService — 관리자 수동 CRUD
- **What:** 관리자가 공모전/박람회를 수동으로 등록/수정/삭제하는 화면 + API.
- **Why:** 외부 API 수집 데이터에 반복적인 오류/중복이 발견될 때 즉시 수동 교정할 수단이 필요.
- **Pros:** 데이터 오류를 즉시 대응 가능. 향후 수동 등록 소스(사용자 제보 등) 추가 시 기반이 됨.
- **Cons:** 관리자 인증/권한 분리, 별도 화면 구현 부담. v1 시점엔 사용 빈도가 낮을 것으로 예상.
- **Context:** CEO 리뷰 D3.1에서 "데이터가 API로 자동 수집되므로 사용자가 명시 요청 안 함"으로 연기. Contest 엔티티/Repository는 그대로 재사용 가능.
- **Depends on:** 없음 (독립적으로 언제든 추가 가능).

## 2. DuplicateContestResolver — 소스 간 중복 판정
- **What:** 서울시/KOCCA 등 서로 다른 소스에 동일 공모전이 각각 등록된 경우 제목 정규화 + 기간 겹침 비교로 매칭해 `duplicateOfId` 설정.
- **Why:** 소스가 늘어나면(커넥터 구조로 확장 예정) 중복 노출로 사용자 경험이 나빠질 수 있음.
- **Pros:** 검색 결과 품질 향상, 소스 확장 시 필수가 됨.
- **Cons:** 제목 정규화/기간 비교 로직 전체 구현 필요, fuzzy matching은 특히 오탐 위험.
- **Context:** Eng 리뷰 Issue 1(Step 0 복잡도 체크)에서 "서울시 문화행사 vs KOCCA 지원사업은 성격이 달라 실제 중복 확률 낮음" 가정으로 연기. 단, 이 가정은 **사전 스파이크**(0단계 이전, 아웃사이드 보이스 지적)에서 실제 샘플로 검증 예정 — 검증 결과 중복률이 예상보다 높으면 이 항목의 우선순위를 올려야 함.
- **Depends on:** 사전 스파이크의 중복률 검증 결과.

## 3. SavedSearch — 자유 키워드 저장검색 알림
- **What:** 관심분야 enum을 넘어 자유 키워드로 검색 조건을 저장하고, 조건에 맞는 신규 공모전이 등록되면 알림.
- **Why:** CEO 리뷰의 Approach C(개인화 알림 차별화)의 핵심 — 기존 경쟁 서비스(씽굿/올콘/링커리어) 대비 가장 강한 차별화 지점.
- **Pros:** 검색엔진 수준의 개인화, 진짜 차별화 포인트.
- **Cons:** 키워드 매칭/저장 엔진 별도 설계 필요, v1 범위(백엔드/DB 중심) 대비 훨씬 큼.
- **Context:** CEO 리뷰 D3.4에서 연기. `KeywordEnumNormalizer`가 먼저 자리잡은 뒤(정규화 로직 안정화 이후) 착수하면 재사용 가능한 기반이 있음.
- **Depends on:** 없음, 다만 `KeywordEnumNormalizer` 및 정규화 폴백률이 낮게 안정된 뒤 착수 권장.

## 4. Refresh Token 지원
- **What:** Access Token 만료 시 재로그인 없이 Refresh Token으로 갱신.
- **Why:** 현재는 Access Token 만료(1시간) 시 무조건 재로그인 필요 — 사용자 경험 저하.
- **Pros:** 로그인 유지 시간 연장, UX 개선.
- **Cons:** Refresh Token 저장/회전/탈취 대응 로직 추가 필요, 보안 설계 복잡도 증가.
- **Context:** 최초 계획부터 MVP 범위 밖으로 명시. JWT 아키텍처를 유지하기로 확정(Eng 리뷰 Issue 1)했으므로 자연스러운 다음 단계.
- **Depends on:** 없음.

## 5. 정식 Rate Limiter (Bucket4j/Redis, IP 단위)
- **What:** 로그인/회원가입 엔드포인트에 IP 단위 rate limiting을 외부 인프라(Bucket4j+Redis 등)로 구현.
- **Why:** v1은 계정별 실패 횟수와 잠금 시각을 PostgreSQL에 저장하여 재시작 후에도 유지하지만, IP 단위 방어는 제공하지 않음(5회 실패 시 15분 잠금).
- **Pros:** 분산 환경 대응, 더 정교한 공격 방어.
- **Cons:** 새 인프라(Redis) 도입 필요, 개인 프로젝트 초기 스케일엔 과함.
- **Context:** Eng 리뷰 Issue 4의 외부 인프라 추가 보류는 유지. 실제 구현은 인메모리 대신 DB에 실패 횟수/잠금 시각을 저장하고 계정 행 잠금으로 동시 로그인 시도를 처리함.
- **Depends on:** 없음, 트래픽 증가 시 우선순위 상승.

## 6. 배포/운영 인프라 (호스팅/도메인/운영 SMTP/CI)
- **What:** 실제 서비스 배포 대상(호스팅), 도메인, 운영용 SMTP 발신 서비스, CI 파이프라인 구성.
- **Why:** 현재는 로컬 개발(실행 스크립트, PostgreSQL, Mailpit)까지 검증 범위 — 실사용자에게 선보이려면 배포 경로가 필요.
- **Pros:** 실사용자 검증 가능, "국내 공모전 통합 검색"이라는 제품 약속을 실제로 시험해볼 수 있음.
- **Cons:** 호스팅/도메인 선택은 비용·운영 부담이 달라 사용자의 실제 선택이 필요.
- **Context:** 아웃사이드 보이스(크로스모델 텐션 2)가 지적. 로컬 MVP 동작 확인 후 논의하기로 함.
- **Depends on:** MVP 구현 완료(0~8단계) 및 사전 스파이크.

## 7. 목록/상세 부가 기능 (공유 버튼, 최근 조회 히스토리, 마감임박순 기본 정렬)
- **What:** 원본 링크 공유 버튼, 마이페이지 최근 조회 공모전 히스토리, 검색 결과 기본 정렬을 마감임박순으로.
- **Why:** CEO 리뷰에서 델라이트 후보로 식별했으나 우선순위상 하위로 분류되어 개별 질의하지 않음.
- **Pros:** 각각 저비용 UX 개선.
- **Cons:** v1 핵심 목표(검색/알림)에 직접 기여하지 않음.
- **Context:** CEO 리뷰 0D 단계에서 "하위 우선순위, 미질의 — 사용자가 원하면 요청 가능"으로 분류.
- **Depends on:** 없음.
